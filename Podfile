# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MemobirdDiary' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MemobirdDiary
  pod 'TouchDraw'
  target 'MemobirdDiaryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MemobirdDiaryUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
